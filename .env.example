# Optional. A GitHub personal access token (no scopes needed for public data)
# raises the rate limit from 60 to 5,000 requests/hour.
# NOTE: anything prefixed VITE_ is bundled into the client, so use a throwaway
# read-only token and never commit a real one.
VITE_GITHUB_TOKEN=
